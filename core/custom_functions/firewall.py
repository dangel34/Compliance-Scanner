def firewall_enabled(): # Test Only lol
    # net localgroup administrators
    # they would have to run everything like subprocess and such/and the scanner will only read the results
    return True
